{
  "name": "thauma-site",
  "version": "1.0.0",
  "description": "Thauma ministry website — Eleventy + Decap CMS + Netlify",
  "scripts": {
    "build": "node -e \"require('fs').rmSync('_site',{recursive:true,force:true})\" && eleventy",
    "dev": "eleventy --serve --port 8123"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "netlify-cli": "^26.2.0"
  }
}
