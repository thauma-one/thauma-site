---
layout: layouts/staff.njk
staffPage: support
staffNum: "02"
staffTitle: Support
staffHeading: Goals and <b>progress</b>
staffHint: aggregates only — no donor records held
---
{% set embedWidget = "goal" %}
{% include "embed-panel.njk" %}

{#- Wrapped so the view switch can take it away: the widget shows the SAME
   goals, and two sets of cards on one screen would just invite comparison. -#}
<div data-emb-content>
<div class="goals" id="goalGrid"></div>

<p class="note" data-i18n-html="sup.note"><b>This holds no donor records.</b> Only four numbers per goal — raised, donor count,
  source and captured-at — pulled from the giving platform. Donor detail lives there, and
  a partner reads their own by logging into it directly.</p>
</div>
